<div class="p2">

        <table class="table">
            <thead class="table-dark">
                <tr>
                    <th>#</th>      
                    <th>Quantity</th>        
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                
            <tr>
                <td>Entire Cart #{{cart.id}} </td>
                <td></td>
                <td>                                  
                    <button class="deleteCart btn btn-danger" id="{{this.id}}" data-id="{{@index}}">Delete All Card Products</button>
                </td>
            </tr>
            {{#each productsData}}
            <tr>
                <td>Cart #{{this._productId}} </td>
                <td>Products in cart {{this.product}}</td>

                <td>
                    <a href="/add/{{this._productId}}" class="btn btn-success">Edit</a>
                    {{!-- <a href="/api/carts/deleteCart/{{this.id}}" class="btn btn-danger">Delete</a> --}}                                        
                    <button class="deleteCart btn btn-danger" id="{{this.id}}" data-id="{{@index}}">Delete</button>

                </td>
            </tr>
            
            {{/each}}
            </tbody>

        </table>
    </div>
<script src="/js/editCart.js"></script>